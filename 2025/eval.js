import { table } from "table.js"
/** @param {NS} ns */
export async function main(ns) {
  var a = [1, 2]
  var b = [2, 3]
  ns.tprint(
    a * b
    // ns.formatNumber(ns.getPurchasedServerCost(ns.args[0]))
  )
}